#!/usr/bin/env bash

# install docker

# create directories with propper permissions

# default location for configs
mkdir /etc/cyral
chmod 
# symlink scripts
ln -s "${PWD}/crawler" /usr/bin/crawler
ln -s "${PWD}/crawler-job" /usr/bin/crawler-job
